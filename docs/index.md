---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stack Provider"
description: |-
  A provider for managing Nuon install stacks.
---

# stack Provider

Read Nuon install-stack configuration and report run status from Terraform.

## Example Usage

```terraform
terraform {
  required_providers {
    stack = {
      source = "nuonco/stack"
    }
  }
}

provider "stack" {
  # Base URL of the Nuon API. Defaults to https://api.nuon.co.
  # Override with your control plane's domain if using Nuon BYOC.
  # api_url = "https://api.nuon.my-domain.com"
}
```

<!-- schema generated by tfplugindocs -->

## Schema

### Optional

- `api_url` (String) Base URL of the Nuon API, up to but excluding `/v1`. Defaults to `https://api.nuon.co`.
